# Based on https://github.com/wincent/wincent/blob/main/aspects/dotfiles/files/.zsh/aliases
# alias :e=vim
# alias :qa=exit
# alias :sp='test -n "$TMUX" && tmux split-window'
# alias :vs='test -n "$TMUX" && tmux split-window -h'
# alias :wq=exit
# alias b=brew
# alias g=git
# alias o='git oneline'
# alias p='git patch'
# alias t=tmux
# alias v=vim
#
# if command -v nvim &> /dev/null; then
#   alias vim=nvim # Use `\vim` or `command vim` to get the real vim.
# fi

alias word='shuf -n1 /usr/share/dict/words'
# TODO: to actually make this generate more secure passphrases (ones that are
# not susceptible to dictionary attacks), we could either:
# - Use a wordlist like EFF's wordlist
# - Take the output of this alias and add/remove/change random characters
alias phrase='shuf -n5 /usr/share/dict/words | tr "\n" " "'
